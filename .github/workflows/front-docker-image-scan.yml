name: Frontend Image Scanner
on:
    # push:
    #     branches: [actions]
    workflow_call:
jobs:
    frontend_image_scanner:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Code
              uses: actions/checkout@v4
              
            - name: Login to Docker Hub
              uses: docker/login-action@v3
              with:
                username: ${{ vars.DOCKER_HUB_USERNAME }}
                password: ${{ secrets.DOCKER_PASSWORD }}

            - name: Trivy Scanner for Frontend Image
              uses: aquasecurity/trivy-action@0.35.0
              with:
                image-ref: ${{ vars.DOCKER_HUB_USERNAME }}/frontend:${{ github.sha}}

